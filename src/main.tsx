import React from 'react';
import ReactDOM from 'react-dom/client';
import TolMaster from './components/TolMaster';
import './app/globals.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
        <TolMaster />
    </React.StrictMode>
);
